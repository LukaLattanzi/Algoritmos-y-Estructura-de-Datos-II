-- Clases de tipos basicas:

-- Eq: (==) y (/=)
-- Ord: (<), (>), (<=), (>=)
-- Show: (show) -> convierte un valor en una cadena de texto

-- Read: (read) -> convierte una cadena de texto en un valor
-- > read "True" || False -> True

-- Enum: (succ), (pred), (toEnum), (fromEnum) -> convierte un valor en su sucesor o predecesor, o viceversa
-- Bounded: (minBound), (maxBound) -> devuelve el valor mínimo o máximo de un tipo enumerado
-- Num: (+), (-), (*), (abs), (signum) -> operaciones aritméticas básicas
-- Integral: (div), (mod), (quot), (rem) -> operaciones aritméticas enteras

